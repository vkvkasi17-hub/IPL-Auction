'use client';
import {useState,useEffect,type CSSProperties} from 'react';
import {ArrowRight,ArrowLeft,Bot,Users,Gavel,Sparkles,Trophy,Check,Ticket,Wallet} from 'lucide-react';
import {teams} from '@/lib/game';
import {Button} from '@/components/ui/button';
import {Input} from '@/components/ui/input';
type Props={savedRoom:string;resume:()=>void;team:string;setTeam:(s:string)=>void;name:string;setName:(s:string)=>void;mode:string;setMode:(s:string)=>void;code:string;setCode:(s:string)=>void;busy:boolean;error:string;enter:()=>void};
export function Entrance(p:Props){
 const [step,setStep]=useState(0);
 useEffect(()=>{window.scrollTo(0,0)},[step]);
 const selected=teams.find(t=>t.id===p.team)!;
 return <div className="entrance"><header className="entry-header"><a className="brand" href="/"><span className="brand-icon"><Gavel size={22}/></span>mock auction<span className="brand-dot">.</span></a><span className="entry-pill"><Sparkles size={14}/>THE GRAND CRICKET AUCTION</span></header><main className="entry-main">
 {step===0?<section className="welcome"><div className="welcome-copy"><div className="eyebrow"><span/>YOUR SEAT AT THE BIG TABLE</div><h1>Big stage.<br/>Bigger <span>ambitions.</span></h1><p>The lights are on. The paddles are ready.<br/>Build your dream cricket team, one winning bid at a time.</p><Button className="enter-button" onClick={()=>setStep(1)}>Enter the grand auction <ArrowRight size={20}/></Button><div className="resume-row">{p.savedRoom&&<button disabled={p.busy} onClick={p.resume}>{p.busy?'Opening…':'Rejoin previous room'} <ArrowRight size={14}/></button>}{p.error&&<p role="alert" className="error">{p.error}</p>}</div><div className="welcome-stats"><span><b>10</b>iconic franchises</span><span><b>60</b>star players</span><span><b>₹120 Cr</b>your starting purse</span></div></div><div className="welcome-art" aria-hidden="true"><div className="orb-ring"/><div className="grand-ticket"><div className="ticket-top"><Sparkles size={20}/><span>ALL ACCESS · AUCTION NIGHT</span></div><div className="trophy-orb"><Trophy size={100} strokeWidth={1.2}/></div><h2>Your team.<br/>Your legacy.</h2><div className="ticket-bottom"><span>ADMIT ONE<br/><b>TEAM OWNER</b></span><Gavel size={32}/></div></div><div className="floating-chip chip-one"><span>RCB</span> Make your move.</div><div className="floating-chip chip-two"><span>CSK</span> Go for glory.</div><div className="floating-chip chip-three"><span>MI</span> Build a dynasty.</div></div></section>:<section className="entry-flow"><div className="flow-navigation"><button onClick={()=>setStep(step-1)} disabled={p.busy}><ArrowLeft size={17}/>Back</button><div className="step-dots"><span className="done">01 · Arrive</span><i/><span className={step>=1?'done':''}>02 · Franchise</span><i/><span className={step===2?'done':''}>03 · Rivals</span></div></div><div className="flow-heading"><span className="eyebrow">{step===1?'FIRST, PICK YOUR COLORS':'NEXT, CHOOSE YOUR CHALLENGE'}</span><h1>{step===1?'One team. All yours.':'Who’s across the table?'}</h1><p>{step===1?'Ten franchises. A fresh start. Choose the team you’ll take to the top.':'Invite your friends or take on nine computer managers. You control when it begins.'}</p></div>
 {step===1?<><div className="franchise-grid">{teams.map(t=><button aria-pressed={p.team===t.id} className={`franchise-tile ${p.team===t.id?'chosen':''}`} style={{'--team-color':t.color} as CSSProperties} key={t.id} onClick={()=>p.setTeam(t.id)}><div className="tile-top"><span>{t.city}</span>{p.team===t.id?<Check size={19}/>:<span className="choice-ring"/>}</div><strong>{t.id}</strong><h2>{t.name}</h2><div className="tile-foot">{p.team===t.id?'YOUR FRANCHISE':'CLAIM YOUR SEAT'}<ArrowRight size={16}/></div></button>)}</div><div className="flow-bottom panel"><span><span className="muted">YOUR PICK</span><b>{selected.name}</b></span><Button className="enter-button" onClick={()=>setStep(2)}>Choose opponents <ArrowRight/></Button></div></>:<div className="challenge-layout"><div className="challenge-options"><button className={`challenge-card ${p.mode==='solo'?'chosen':''}`} onClick={()=>p.setMode('solo')} aria-pressed={p.mode==='solo'}><span className="challenge-icon"><Bot size={32}/></span><span className="challenge-copy"><small>YOUR SOLO SPOTLIGHT</small><h2>Vs computer</h2><p>Nine computer managers. One ambitious you. Practice your strategy at the big table.</p></span>{p.mode==='solo'&&<Check/>}</button><button className={`challenge-card pink ${p.mode==='friends'?'chosen':''}`} onClick={()=>p.setMode('friends')} aria-pressed={p.mode==='friends'}><span className="challenge-icon"><Users size={32}/></span><span className="challenge-copy"><small>RIVALRIES, REIMAGINED</small><h2>Vs friends</h2><p>Create a private room for up to nine friends. Computers fill the remaining seats when you start.</p></span>{p.mode==='friends'&&<Check/>}</button><button className={`join-choice ${p.mode==='join'?'chosen':''}`} onClick={()=>p.setMode('join')} aria-pressed={p.mode==='join'}><Ticket size={22}/><span>Already invited? <b>Join with a room code</b></span><ArrowRight size={18}/></button></div><section className="entry-details panel"><div className="eyebrow">YOUR AUCTION PASS</div><div className="your-choice" style={{'--team-color':selected.color} as CSSProperties}><span className="team-badge">{selected.id}</span><div><small>TEAM OWNER</small><b>{selected.name}</b></div></div><label htmlFor="manager">Manager name</label><Input id="manager" autoComplete="nickname" placeholder="What should we call you?" maxLength={24} value={p.name} onChange={e=>p.setName(e.target.value)}/>{p.mode==='join'&&<><label htmlFor="invite">Room code</label><Input id="invite" placeholder="8-character room code" maxLength={8} value={p.code} onChange={e=>p.setCode(e.target.value.toUpperCase())}/></>}<div className="pass-note"><Wallet size={20}/><span>₹120 Cr to build your dream squad.<br/>The auction waits for the host to start.</span></div>{p.error&&<p role="alert" className="error">{p.error}</p>}<Button className="enter-button" disabled={p.busy||!p.name.trim()||(p.mode==='join'&&!/^[A-F0-9]{8}$/i.test(p.code))} onClick={p.enter}>{p.busy?'Opening your room…':p.mode==='join'?'Join auction room':'Enter auction room'}<ArrowRight/></Button></section></div>}
 </section>}
 <footer className="entry-footer"><span>Mock Auction · Your team. Your call.</span><span>Fan-made fantasy cricket · Not affiliated with IPL or BCCI</span></footer></main></div>
}
